============
Installation
============

At the command line::

    $ easy_install django-resource-scheduler

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-resource-scheduler
    $ pip install django-resource-scheduler
